aux_source_directory(./ SRC_FILE)
include_directories(${HEADPATH})
link_directories(${LIBPATH})
link_libraries(${SOCKETLIB})
link_libraries(${SETSIDLIB})
set(EXECUTABLE_OUTPUT_PATH ${EXECPATH})
add_executable(${APPNAME2} ${SRC_FILE})
